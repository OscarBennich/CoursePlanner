stages:
    - build
    - test
build:
    stage: build
    script:
        - '"nuget.exe" restore'
        - '"msbuild.exe" /p:VisualStudioVersion=15.0'
    artifacts:
        untracked: true
test:
    stage: test
    script:
        - '"mstest.exe" /testcontainer:testproj.Tests/bin/Debug/testproj.Tests.dll'
